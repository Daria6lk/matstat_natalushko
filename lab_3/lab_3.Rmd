---
title: "Математична статистика. Лабораторна робота 3. Основи вибіркового методу"
author: "Наталушко Д.О."
date: "`r Sys.Date()`"
output: 
  html_notebook:
    toc: true
    toc_float: true
    highlight: tango
fontsize: 12pt
header-includes:
  - \usepackage[T2A]{fontenc}
  - \usepackage[utf8]{inputenc}
  - \usepackage[russian]{babel}
editor_options:
  chunk_output_type: console
  markdown: 
    wrap: 72
bibliography: references_lab.bib
---

## Постановка задачі

Розглядається випадкова величина $X$, що має нормальний розподіл з
параметрами:

-   математичне сподівання $a = 3$,
-   стандартне відхилення $\sigma = 2$.

Мета роботи:

1.  Згенерувати вибірку з розподілу $\mathcal{N}(3, 2)$ обсягом:

    -   $n = 100$,

2.  Обчислити вибіркові оцінки параметрів розподілу та основні
    статистичні характеристики (середнє, дисперсію, стандартне
    відхилення, асиметрію, ексцес тощо).

3.  Провести порівняльний аналіз оцінених характеристик між двома
    вибірками та з теоретичними значеннями параметрів.

4.  Виконати візуалізацію вибіркових даних за допомогою гістограми,
    емпіричної функції розподілу, боксплоту та оцінки щільності.

------------------------------------------------------------------------

Далі в документі реалізується генерація вибірок, обчислення статистичних
характеристик та побудова графіків.

## Генерація вибірки

```{r}
if (!requireNamespace("beeswarm", quietly = TRUE)) {
  install.packages("beeswarm")
}
library(beeswarm)
set.seed(0) # фіксація генератора
a <- 3      # математичне сподівання
s <- 2      # стандартне відхилення
n <- 100    # обсяг вибірки

X <- rnorm(n, a, s) # генерація вибірки

cat("\nПараметри вибірки:\n", "a = ", a, "\n", "s = ", s, "\n", "n = ", n, "\n")
cat("Перші елементи вибірки:", head(X), "\n")
cat("Останні елементи вибірки:", tail(X), "\n")

```

## Вивід основних статистичних характеристик

```{r}
summary(X)
```

## Візуалізація вибіркових характеристик

```{r}
op <- par(mfrow = c(2,2)) # 2x2 макет

# 1. Гістограма + теоретична щільність
hist(X, freq = FALSE, col = "lightgray", main = "Histogram", border = 4)
curve(dnorm(x, a, s), col = 2, lty = 2, lwd = 2, add = TRUE)

# 2. Емпірична функція розподілу
Fn <- ecdf(X)
plot(Fn, main = "Quantile Plot", verticals = TRUE,
     col.points = "blue", col.hor = "red", col.vert = "bisque",
     xlab = "X", ylab = "Fn(x)")

# 3. Boxplot + "рій"
boxplot(X, main = "Box-and-Whisker Plot", col = "lightgray", border = 4,
        xlab = "X", horizontal = TRUE)
beeswarm(X, col = 2, add = TRUE, horizontal = TRUE)

# 4. Щільність розподілу
plot(density(X, adjust=2), main = "Density Trace", xlab = "X", ylab = "Density", col = "blue")

rug(X,
col=2,
main="fn(x)") # Виділення точками місць з найбільшою щільністю значень

par(op) #Повертаємо параметри за замовчанням
```

## Висновки

Згенерована вибірка з обсягом 𝑛 = 100 має вибіркове середнє 𝑥 ˉ = 3.045
x ˉ =3.045, що дуже близько до теоретичного значення 𝑎 = 3 a=3, що
свідчить про коректність генерації даних.

Діапазон значень вибірки від -1.448 до 7.883 відповідає очікуваному
розмаху для нормального розподілу з параметрами 𝑎 = 3 a=3 та 𝜎 = 2 σ=2.

Медіана вибірки 2.934 2.934 також близька до математичного сподівання,
що вказує на симетричність розподілу вибіркових даних.

Значення першого квартиля (1.861) та третього квартиля (4.251) показують
нормальний розподіл даних навколо центру.

На основі цих даних можна зробити попередній висновок, що вибіркова
вибірка адекватно моделює теоретичний нормальний розподіл 𝑁 ( 3 , 2 )
N(3,2).
